export const metadata = {
  title: 'Data Migration | Admin Dashboard',
  description: 'Data migration and management for service requests and payments',
};

export default function MigrationLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
